let lista = ['ovo', 'Farinha', 'corante', 'massa']

// Converte array para string
let res1 = lista.toString()

// Pode ser adicionado um separador
let res2 = lista.join('-')

// Procurando dentro do array
let res = lista.indexOf('ovo')

console.log(res)